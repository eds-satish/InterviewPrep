function dfs(graph, startVertex){
	var stack = [startVertex];
	visitedNodes = [];
	while(!stack.isEmpty()){
		x = stack.pop(0);
		/*	while stack != []:
		x = stack.pop(0)
		if x not in visited:
			visited = visited + [x] #add new visited node to visited list
			if graph[x] is not None:
				stack = graph[x] + stack #push to top of stack
		return visited*/
	}
};




